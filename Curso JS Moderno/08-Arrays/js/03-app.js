// operaciones. Como acceder a todos los elementos de un arreglo

const meses = ['Enero', 'Febrero', 'Marzo', 'Abril', 'Mayo', 'Junio', 'Julio'];
console.table(meses);

//recorrer el arreglo. ¿Cuanto mide el arreglo?
console.table(meses.length);

// iterador

for(let i=0; i < meses.length; i++){
    console.log(meses[i]);
}