/**
 * crear algoritmo que imprima numero impares
 */

let impares = 0;

for (let impares=0; impares < 10; impares ++){
    if ( impares % 2 != 0){
        console.log('Numero impar', impares);
    }
    impares ++;
}