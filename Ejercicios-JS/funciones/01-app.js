/* Declarar una función completaName y ahora toma el nombre primero, lastName como parámetro y devuelve su nombre completo.
const name1 = 'sara';
const completeName = function(lastName){
    console.log(`Tu nommbre completo es: ${name1} ${lastName}`)
}
completeName('gonzalez')*/

/* Declarar una función addNumbers y toma dos parámetros y devuelve la suma.

const addNumbers = function(num1, num2){
    let total = num1 + num2;
    console.log(`la suma de ${num1} + ${num2} es: ${total}`)
}
addNumbers(2, 5) */

/* Una zona de un rectángulo se calcula de la siguiente manera: área = longitud x anchura. Escriba una función que calcule el áreaDeRectangle.

const areaRectangulo = function(long, anchura){
    let area = long * anchura;
    console.log(`El área del rectangulo es: ${area}`)
}

areaRectangulo(5, 10);*/
